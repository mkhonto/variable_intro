var theCommu = "unity feeds to emancipation"; // here i declared a string
console.log(theCommu.length); // i going to print the length of a string

theCommu = 456; // this string is changed to a number
console.log("theCommu.length is now undefined and your program might break!"); // this will fail
console.log(theCommu.length);
